# prep-javascript-objects-quiz-notes

## Quiz Questions

Answer the following questions in the provided markdown file before turning in this exercise:

- What data types can object properties hold?
  it can hold any data type that JavaScript utilizes
- Describe the syntax (structure) of object-literals in JavaScript.
  You first have the object name at the top declared as a variable and then you add attributes or key value pairs separated by commas inside of them within the {}

## Notes

All student notes should be written here.

How to write `Code Examples` in markdown

for JS:

```javascript
const data = 'Howdy';
```

for HTML:

```html
<div>
  <p>This is text content</p>
</div>
```

for CSS:

```css
div {
  width: 100%;
}
```
